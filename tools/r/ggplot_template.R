# ggplot starter template
library(ggplot2)

# Replace with your dataset load step
# df <- read.csv("path/to/data.csv")

# Example chart template
# ggplot(df, aes(x = x_var, y = y_var)) +
#   geom_col(fill = "steelblue") +
#   labs(
#     title = "Chart Title",
#     x = "X Label",
#     y = "Y Label"
#   ) +
#   theme_minimal()
